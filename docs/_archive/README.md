# Archived Docs

These documents describe older plans or retired surfaces. They are kept for reference only and are not part of the v0.2.0 product contract.

For current behavior, use the active docs in `docs/` and `docs/architecture_docs/`.
