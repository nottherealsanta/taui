- ## Database Schema
    Define the data models and relationships.
    - {{status: ready}}

    - ## Users Table
        Store user information.
        - {{status: done}}
        - {{code_ref: `src/database.py#L1-L20`}}

    - ## Tasks Table
        Store task records.
        - {{status: done}}
        - {{code_ref: `src/database.py#L22-L45`}}
